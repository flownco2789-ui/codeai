name: Deploy (prod)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: codeai-prod-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, codeai-prod]
    steps:
      - name: Deploy on server
        run: sudo /usr/local/bin/deploy-codeai.sh
