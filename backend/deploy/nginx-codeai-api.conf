# /etc/nginx/sites-available/codeai-api.conf
# 1) api.codeai.co.kr → 110.165.16.40 A 레코드 설정
# 2) certbot 으로 인증서 발급 후, 443 server 블록을 활성화

server {
    listen 80;
    server_name api.codeai.co.kr;

    # certbot 사용 시 보통 아래 라인을 활성화합니다.
    # location /.well-known/acme-challenge/ { root /var/www/html; }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS 예시(인증서 경로는 certbot 결과에 맞게 수정)
# server {
#     listen 443 ssl http2;
#     server_name api.codeai.co.kr;
#
#     ssl_certificate /etc/letsencrypt/live/api.codeai.co.kr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.codeai.co.kr/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
